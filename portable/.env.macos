# ===========================================
# Mail Agent Configuration - macOS (M3 Pro)
# ===========================================

# Email Server Configuration
# --------------------------
# Change these to your mail server details
IMAP_HOST=localhost
IMAP_PORT=993
IMAP_USE_SSL=true
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USE_TLS=true

# Email Credentials
# -----------------
# The mailbox the AI agent monitors
EMAIL_ADDRESS=company@mail.local
EMAIL_PASSWORD=your_password_here

# AI Provider: Ollama (running locally on Mac)
# --------------------------------------------
# For local Ollama on the same Mac:
OLLAMA_HOST=http://localhost:11434

# Recommended model for M3 Pro 18GB:
# - qwen2.5:7b   (best balance of speed/accuracy)
# - qwen2.5:14b  (best accuracy, slower)
# - qwen2.5:3b   (fastest, good accuracy)
OLLAMA_MODEL=qwen2.5:7b

# Optional: Google Gemini (cloud alternative)
# -------------------------------------------
# Leave empty to use Ollama instead
GEMINI_API_KEY=
