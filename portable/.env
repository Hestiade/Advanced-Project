# Email Server Configuration (Maddy Mail Server)
IMAP_HOST=localhost
IMAP_PORT=143
IMAP_USE_SSL=false
IMAP_USE_STARTTLS=true
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USE_TLS=true

# Email Credentials (company inbox - monitored by AI agent)
EMAIL_ADDRESS=company@mail.local
EMAIL_PASSWORD=company01

# Test sender account (for sending test emails)
TEST_SENDER_EMAIL=deniz@mail.local
TEST_SENDER_PASSWORD=deniz01

# AI Provider: Ollama (running on Windows host with GPU)
OLLAMA_HOST=http://192.168.1.107:11434
OLLAMA_MODEL=qwen3:14b

# Option 2: Google Gemini (requires API key)
GEMINI_API_KEY=
